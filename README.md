# Telegram Бот "Золотая рыбка" v2.0

## Описание функционала

Telegram бот "Золотая рыбка" - это комплексное решение для заказа рыбы на базе Flask, состоящее из следующих компонентов:

1. **Telegram Bot** - Основной интерфейс взаимодействия с пользователями
   - Бот отправляет приветственное сообщение с ссылкой на Telegram Mini App
   - Пользователи оформляют заказы через Mini App
   - При команде `/admin` администраторам отправляется ссылка на веб-интерфейс

2. **Telegram Mini App** - Интегрированное веб-приложение внутри Telegram для оформления заказов
   - Адаптивный интерфейс для выбора товаров
   - Простая система оформления заказа
   - Автоматическое создание публичных URL через cloudflared

3. **Веб-приложение администратора** - Панель управления на Flask
   - Просмотр всех заказов и управление ими
   - Отслеживание статусов заказов
   - Экспорт данных в Excel
   - Автоматическое создание публичных URL через cloudflared

4. **Сервис синхронизации** - Фоновый процесс для обработки данных
   - Обработка устаревших лидов
   - Синхронизация статусов заказов
   - Очистка старых данных

## Новые возможности v2.0

- ✅ **Полная совместимость с Windows** - убран gunicorn, используется чистый Flask
- ✅ **Автоматические публичные URL** - интеграция с cloudflared для создания туннелей
- ✅ **Улучшенное логирование** - отображение URL для доступа к сервисам
- ✅ **Упрощенная структура** - все файлы в корневой папке
- ✅ **Автоматическое управление портами** - освобождение занятых портов
- ✅ **Стабильная работа процессов** - улучшенный мониторинг и перезапуск

## Файловая структура проекта

- **run.py** - Главный файл для запуска всей системы
- **botf.py** - Код Telegram бота
- **main.py** - Код веб-приложения администратора на Flask
- **sync_service.py** - Код сервиса синхронизации
- **static/** - Статические файлы (CSS, JavaScript, изображения)
  - **css/** - Стили для веб-приложения
  - **img/** - Изображения
  - **webapp/** - Файлы для Telegram Mini App
  - **exports/** - Папка для экспортируемых данных
- **templates/** - HTML-шаблоны для веб-приложения
- **requirements.txt** - Список требуемых Python-пакетов
- **bot.db** - SQLite база данных для хранения данных бота

## Инструкция по установке и запуску

### Предварительные требования

1. **Python 3.7+** - Установите Python с [официального сайта](https://www.python.org/downloads/)
2. **Pip** - Установщик пакетов Python (обычно устанавливается вместе с Python)
3. **Cloudflared** (рекомендуется) - Для автоматического создания публичных URL

### Шаг 1: Установка cloudflared (рекомендуется)

#### Windows:
```bash
# Скачайте cloudflared с https://github.com/cloudflare/cloudflared/releases
# Или используйте winget:
winget install --id Cloudflare.cloudflared
```

#### macOS:
```bash
brew install cloudflared
```

#### Linux:
```bash
# Ubuntu/Debian:
wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
sudo dpkg -i cloudflared-linux-amd64.deb

# Или через snap:
sudo snap install cloudflared
```

### Шаг 2: Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### Шаг 3: Настройка Telegram бота

1. Создайте нового бота у [@BotFather](https://t.me/BotFather) в Telegram
2. Получите и скопируйте токен бота
3. Откройте файл `botf.py` в текстовом редакторе
4. Замените значение переменной `TOKEN` на ваш токен:
   ```python
   TOKEN = "ваш_токен_бота"
   ```
5. Замените `ADMIN_IDS` на ID ваших администраторов:
   ```python
   ADMIN_IDS = [ваш_id_в_telegram]
   ```
   (Узнать свой ID можно у бота [@userinfobot](https://t.me/userinfobot))

### Шаг 4: Запуск системы

```bash
python run.py
```

Система автоматически:
- Запустит Telegram бота
- Запустит сервис синхронизации данных
- Запустит веб-приложение администратора на порту 5000
- Запустит Telegram Mini App на порту 8080
- Создаст публичные URL через cloudflared (если установлен)

### Шаг 5: Доступ к сервисам

После запуска в логах вы увидите:

```
📋 Локальные адреса:
   - Админ-панель: http://localhost:5000
   - Mini App: http://localhost:8080
🌐 Публичные URL будут показаны выше после запуска cloudflared
```

Публичные URL для доступа из интернета будут автоматически созданы и отображены в логах.

**Логин и пароль для админ-панели по умолчанию:**
- Логин: admin
- Пароль: admin

## Особенности работы в Windows

- Система полностью адаптирована для Windows
- Используется чистый Flask вместо gunicorn
- Автоматическое управление процессами через taskkill
- Поддержка всех функций без дополнительных зависимостей

## Устранение неполадок

### Порты заняты
Система автоматически освобождает занятые порты 5000 и 8080 при запуске.

### Cloudflared не найден
Если cloudflared не установлен, система будет работать только с локальными адресами. Для доступа из интернета установите cloudflared или используйте альтернативные решения туннелирования.

### Бот не отвечает
1. Убедитесь, что токен бота правильно настроен в `botf.py`
2. Проверьте логи на наличие ошибок
3. Перезапустите систему командой `python run.py`

### Веб-приложение недоступно
1. Проверьте, что порт 5000 не заблокирован файерволом
2. Убедитесь, что процесс Flask запустился (проверьте логи)
3. Попробуйте обратиться к http://localhost:5000 напрямую

## Дополнительная информация

- База данных автоматически создается в файле `bot.db` при первом запуске
- Все логи выводятся в консоль с префиксами для каждого сервиса
- Система автоматически перезапускает компоненты при сбоях
- Для остановки всей системы нажмите Ctrl+C в консоли

## Рекомендации по развертыванию

### Локальная разработка и тестирование
Используйте систему как описано выше с cloudflared для публичного доступа.

### Постоянное размещение на VPS
1. Арендуйте виртуальный сервер
2. Установите проект на сервер
3. Настройте автозапуск через systemd (Linux) или службы Windows
4. Используйте nginx для проксирования запросов (опционально)

### Безопасность
- Не размещайте токен бота в публичном доступе
- Измените пароль администратора после первого входа
- При использовании в production настройте HTTPS
- Регулярно создавайте резервные копии базы данных

## Техническая поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки токена бота
4. Убедитесь, что порты 5000 и 8080 доступны

Система "Золотая рыбка" v2.0 обеспечивает стабильную работу на всех платформах с минимальными требованиями к настройке.